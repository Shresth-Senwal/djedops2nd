// export const dynamic = 'force-dynamic';  // Disabled for static export
// export const revalidate = 0;  // Disabled for static export

export default function DjedOpsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
